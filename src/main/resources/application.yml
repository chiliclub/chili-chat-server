spring:
  application:
    name: chili-chat
  profiles:
    group:
      default: dev
      local: local
      dev: dev
      prod: prod
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher